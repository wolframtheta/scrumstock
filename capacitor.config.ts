import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'scrumstock.com',
  appName: 'scrumstock',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
